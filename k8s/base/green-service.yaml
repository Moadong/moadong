apiVersion: v1
kind: Service
metadata:
  name: spring-server-green-svc
spec:
  type: ClusterIP
  selector:
    app: spring-server
    color: green
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080