import * as Styled from './ClubTag.styles';

const ClubTag = () => {
  return (
    <Styled.Container>
      <Styled.ClubText>WAP</Styled.ClubText>
    </Styled.Container>
  );
};

export default ClubTag;
