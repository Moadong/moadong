# CLAUDE.md

전체 작업 가이드는 [`AGENTS.md`](AGENTS.md)에 있다(구조·명령어·스타일·검증·커밋/PR 규칙). 아래는 그중 **놓치면 손해가 큰 3가지**만 옮겨 둔 것이다.

- **착수 전에 열린 PR과 최근 머지를 확인한다.** `gh pr list --state open`, `git fetch origin && git log origin/develop-fe -20`, `git worktree list`. 워크스페이스를 여러 개 병렬로 돌리므로 작업 트리가 깨끗하다고 아무도 안 건드렸다는 뜻이 아니다. 앱은 별도 레포(`Moadong/moadong-react-native`)라 따로 본다. 겹치면 구현하지 말고 먼저 알린다.
- **프론트·백엔드·앱 중 둘 이상에 걸치는 변경은 코드부터 만들지 않는다.** 레포별 필요 변경, 배포 순서, 순서를 어겼을 때의 결과를 먼저 정리해 판단을 받는다.
- **"영향 없다 / 회귀 없다 / 순서 제약 없다"는 근거의 축을 확인하고 쓴다.** 하위 호환 ≠ 데이터 보존, 컴파일·테스트 통과 ≠ 시나리오 커버. 확신이 없으면 조건을 붙여 말한다.

프론트엔드 상세는 [`frontend/CLAUDE.md`](frontend/CLAUDE.md)와 각 폴더의 `CLAUDE.md`에 있다. 해당 폴더의 파일을 작업할 때 자동으로 로드된다.
